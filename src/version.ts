// Single source of truth for the build/release version. Shown in the UI and attached
// to every bug report so an issue is always traceable to an exact build.
// Bump this on each release candidate / release.
export const APP_VERSION = 'v1.0.0-rc2';
